export default function SectionLabel({ label }: { label: string }) {
    return (
        <div>
            {/* TODO: implementar eyebrow label reutilizable */}
            {label}
        </div>
    );
}
