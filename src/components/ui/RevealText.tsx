export default function RevealText({ children }: { children: React.ReactNode }) {
    return (
        <div>
            {/* TODO: implementar wrapper para animación on-scroll */}
            {children}
        </div>
    );
}
